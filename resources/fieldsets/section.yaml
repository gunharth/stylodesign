title: Section
fields:
  -
    handle: slug
    field:
      generate: true
      display: Slug
      type: slug
      icon: slug
      listable: hidden
      validate:
        - required
  -
    handle: slides
    field:
      collapse: false
      sets:
        slides:
          display: Slides
          fields:
            -
              import: slide
      display: Slides
      type: replicator
      icon: replicator
      listable: hidden
